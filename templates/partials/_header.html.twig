  <nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('Home.index')}}">AnimeWatch</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto">
      {% if app.user %}
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('myhome.index')}}">My Home
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
      {% endif %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('anime.index')}}">Animés</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('genres.index')}}">Genres</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('anime_top.index')}}">Tendance</a>
        </li>
      </ul>
      <div class="d-flex">
      
        <input class="form-control me-sm-5" type="search" placeholder="Rechercher...">
     
    <ul class= "navbar-nav me-auto">
        {% if app.user %}
  
        
           <li class="nav-item dropdown" style="margin-right 20rem;" >
          <a class="nav-link dropdown-toggle me-sm-5" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.fullname }}</a>
          <div class="dropdown-menu" >
            <a class="dropdown-item" href="{{ path('user.edit', { id :app.user.id})}}"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Modifier mes informations</font></font></a>
            <a class="dropdown-item" href="{{ path('user.edit.password', { id :app.user.id})}}"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Modifier mon mot de passe</font></font></a>
            <a class="dropdown-item" href="#"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Quelque chose d'autre ici</font></font></a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="{{ path('security.logout')}}"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Déconnexion</font></font></a>
          </div>
        </li>
      
       
        </ul>
        
       {% else %}
        <a href="{{ path('security.login')}}" class="btn btn-tertiary btn-sm me-sm-1">Connexion</a>
        <a href="{{ path('security.registration')}}" class="btn btn-tertiary btn-sm">Inscription</a>

        

      {% endif %}
      </div>
    </div>
  </div>
</nav>
