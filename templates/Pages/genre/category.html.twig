{% extends 'base.html.twig' %}

{% block title %}{{category.name}}{% endblock %}

{% block body %}


<div class = "container mt-4">
    <h1 class= "text-5xl text-center mt-24 mb-8">{{category.name}}</h1>
    <div class="flex flex-row flex-wrap justify-evenly mt-8"><p class = "text-center mt-4">{{category.description}}</p>
       <div class="row mt-4">

        {% for anime in category.animes %}
        
        
        <div class = "col-md-4 mt-4">
         <a class="btn-tertiary" href= "{{ path('anime_name.index', { 'slug' :anime.name}) }}" >
        <div class="card mt-4">
  
  <img src="{{anime.image}}" class="d-block user-select-none" width="100%" height="175">
     <h5 class = "d-flex justify-content-center"><font class=" mt-4 fst-italic">{{anime.name}}</font></h5>
  </svg>
  <text class="text-center"><p>Plus d'information...</p> </text>
  
  
 
   
  
</div>
  </a> 
</div>        
        {% endfor %}
        </div>
        </div>
    </div>
</div>
{% endblock %}