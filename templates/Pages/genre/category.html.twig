{% extends 'base.html.twig' %}

{% block title %}{{category.name}}{% endblock %}

{% block body %}


<div class = "container mt-4">
    <h1 class= "text-5xl text-center mt-24 mb-8">{{category.name}}</h1>
    <div class="flex flex-row flex-wrap justify-evenly mt-8"><p class = "text-center mt-4">{{category.description}}</p>
       <div class="row mt-4">

        {% for anime in category.animes %}
        
        
        <div class = "col-md-4 mt-4">
         <a class="btn-tertiary" href= "{{ path('anime_name.index', { 'slug' :anime.name}) }}" >
        <div class="card mt-4">
  
  <svg xmlns="http://www.w3.org/2000/svg" class="d-block user-select-none" width="100%" height="175" aria-label="Placeholder: Image cap" focusable="false" role="img" preserveAspectRatio="xMidYMid slice" viewBox="0 0 318 180" style="font-size:1.125rem;text-anchor:middle">
    <rect width="100%" height="100%" fill="#868e96"></rect>
    <text x="50%" y="50%" fill="#dee2e6" dy=".3em">{{anime.name}}</text>
  </svg>
  <text class="text-center"><p>Plus d'information...</p> </text>
  
  
 
   
  
</div>
  </a> 
</div>        
        {% endfor %}
        </div>
        </div>
    </div>
</div>
{% endblock %}